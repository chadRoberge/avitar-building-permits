import Controller from '@ember/controller';

export default class AdminLoginController extends Controller {
  // This controller is intentionally minimal - 
  // the AdminLoginForm component handles the login logic
}